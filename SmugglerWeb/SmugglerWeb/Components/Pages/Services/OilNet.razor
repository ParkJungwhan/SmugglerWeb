@page "/Services/OilNetwork"
@using System.Diagnostics
@attribute [StreamRendering]
@inject NavigationManager Nav
@inject PageHeaderState Header

<!-- main -->
content

@code {
    protected override void OnParametersSet()
    {
        // 페이지 진입/파라미터 변화 시 헤더 갱신
        Header.Set("유가 기록 조회 서비스");

        // 필요하면 시간도 직접 지정 가능: Header.Set("보고서", DateTime.UtcNow.ToString("yyyy/MM/dd HH:mm"));
    }
}