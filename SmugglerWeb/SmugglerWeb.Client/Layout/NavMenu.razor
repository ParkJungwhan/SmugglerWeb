@using System.Diagnostics
@rendermode InteractiveAuto

<FluentNavMenu Id="main-menu" Width="300" Collapsible="true" Title="Navigation menu" @bind-Expanded="expanded" CustomToggle="true">
    <FluentNavLink Href="/" Match="NavLinkMatch.All" Icon="@(new Icons.Regular.Size20.Home())" IconColor="Color.Accent">Home</FluentNavLink>
    <!-- Start Private -->
    <FluentNavGroup Icon="@(new Icons.Regular.Size20.EarthLeaf())">
        <TitleTemplate>Who is Smuggler?</TitleTemplate>
        <ChildContent>
       @*      <FluentNavLink OnClick="OnClick" Icon="@(new Icons.Regular.Size20.LeafOne())">Item 3.1</FluentNavLink>
            <FluentNavLink OnClick="OnClick" Icon="@(new Icons.Regular.Size20.LeafTwo())">Item 3.2</FluentNavLink> *@
            <FluentNavLink Href="Counter" Icon="@(new Icons.Regular.Size20.VehicleTruckProfile())" IconColor="Color.Accent">Counter</FluentNavLink>
            <FluentNavLink Href="Weather" Icon="@(new Icons.Regular.Size20.VehicleTruckProfile())" IconColor="Color.Accent">Weather</FluentNavLink>
            <FluentNavLink Href="Private/Chartsample" Icon="@(new Icons.Regular.Size20.VehicleTruckProfile())" IconColor="Color.Accent">chartsample</FluentNavLink>
        </ChildContent>
    </FluentNavGroup>
    <!-- End Private -->
    <!-- Start Public Services  -->
    <FluentNavGroup Icon="@(new Icons.Regular.Size20.EarthLeaf())">
        <TitleTemplate>Services(Authorized)</TitleTemplate>
        <ChildContent>
            <FluentNavLink Href="Services/OilNetwork" Icon="@(new Icons.Regular.Size20.VehicleTruckProfile())" IconColor="Color.Accent">Oil Network</FluentNavLink>
            <FluentNavLink Href="Services/BeatCoin" Icon="@(new Icons.Regular.Size20.VehicleTruckProfile())" IconColor="Color.Accent">'Beat'Coin</FluentNavLink>
        </ChildContent>
    </FluentNavGroup>
    <!-- End Public Services  -->
    <!-- Start Sample -->
    <FluentNavLink Href="About" Icon="@(new Icons.Regular.Size20.NumberSymbolSquare())" IconColor="Color.Accent">About</FluentNavLink>
    <FluentNavLink Href="Guestbook" Icon="@(new Icons.Regular.Size20.NumberSymbolSquare())" IconColor="Color.Accent">Guestbook</FluentNavLink>
    
    <!-- End Sample -->
</FluentNavMenu>

@code {
    private bool expanded = true;    
}